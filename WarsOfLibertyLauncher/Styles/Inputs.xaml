<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ================================================================
         GLOBAL DARK INPUT THEME.

         These are IMPLICIT styles (no x:Key) merged app-wide in App.xaml,
         so every ComboBox / TextBox / CheckBox / RadioButton in the
         launcher gets the dark "Modern Colonial" look automatically
         instead of WPF's default white Aero chrome.

         Why this exists: the stock ComboBox ControlTemplate paints its
         toggle button and dropdown popup with the OS light theme and
         ignores the Background property, so colour-only styles (the old
         per-dialog approach) left the dropdown looking white/un-integrated.
         A full ControlTemplate is the only way to theme a ComboBox.

         Accent: gold (AccentBrush) for focus / selection, matching the
         rest of the launcher chrome. The Multiplayer dialogs keep their
         own blue-accented keyed styles (CreateLobby's MpFormCombo /
         MpFormTextBox / MpCheckBox), which override these by being applied
         explicitly — that blue identity is intentional, see Colors.xaml.
         ================================================================ -->

    <!-- Slim dark scrollbar, scoped to the ComboBox dropdown popup (applied
         via an implicit style inside the popup's ScrollViewer.Resources).
         Keyed so it isn't applied to every scrollbar in the app. -->
    <Style x:Key="DarkComboScrollBar" TargetType="ScrollBar">
        <Setter Property="Width" Value="10"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand"
                                              Focusable="False" IsTabStop="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand"
                                              Focusable="False" IsTabStop="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Background="Transparent"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border CornerRadius="5"
                                                    Background="{DynamicResource BorderSubtle}"
                                                    Margin="2,1"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== ComboBox ===================== -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource BgNeutral}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="MinHeight" Value="32"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource RadiusSm}"/>
                        <!-- Full-surface invisible toggle so a click anywhere
                             opens the dropdown (matches native behaviour). -->
                        <ToggleButton x:Name="ToggleButton"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Background="Transparent"
                                      BorderBrush="Transparent">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent"/>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <Path x:Name="Caret"
                              Data="M 0 0 L 4 4 L 8 0 Z"
                              Fill="{DynamicResource TextSecondary}"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              Margin="0,0,12,0"/>
                        <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               PopupAnimation="Slide"
                               Focusable="False">
                            <Border Background="{DynamicResource BgPanel}"
                                    BorderBrush="{DynamicResource BorderSubtle}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource RadiusSm}"
                                    Margin="0,4,0,0"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="260">
                                <ScrollViewer>
                                    <ScrollViewer.Resources>
                                        <Style TargetType="ScrollBar"
                                               BasedOn="{StaticResource DarkComboScrollBar}"/>
                                    </ScrollViewer.Resources>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                    <Setter Property="Padding" Value="11,7"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="itemBorder"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource TintGoldHover}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="itemBorder" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                        <Setter Property="Foreground" Value="#1A2028"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== TextBox =====================
         WHERE THE CARET LANDS: BorderThickness.Left + Padding.Left, exactly — the
         template maps Padding straight onto PART_ContentHost's Margin and adds nothing
         of its own. That matters because the launcher overlays a placeholder TextBlock
         on several of these, and the placeholder is a SIBLING, so its own Margin has to
         come out to the same number.

         The trap is the border: override only Padding and you silently keep the 1 below,
         so a Padding of 10 puts the caret at 11. That is exactly how the global chat
         composer ended up starting one pixel inside its own placeholder. Two working
         shapes, both already in the repo — pick one, don't invent a third:
           * chrome-less: BorderThickness="0" Padding="0", placeholder Margin 0, the
             containing Border owns the inset (RoomSearchBox, JoinByCodeBox, SearchBox)
           * bordered: BorderThickness="1" Padding="11,0" against a placeholder Margin
             of 12 (ChatInputBox, GlobalChatInput)
         And give the placeholder the SAME FontSize as the box, or the text jumps when
         you start typing even with the insets right.
         ==================================================== -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource BgNeutral}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusSm}">
                        <!-- NO Margin="{TemplateBinding Padding}" here. A TextBox applies
                             its OWN Padding to the text it renders, so binding it onto
                             PART_ContentHost as well counts it TWICE — measured on the chat
                             composer: Padding="11" put the caret 22.4 DIP in, not 11. That
                             doubling is the real reason placeholders overlaid on these boxes
                             never lined up: every call site was tuning a number that was
                             silently multiplied. The idiom IS correct on a ContentPresenter
                             (ComboBox, Button), which does not self-apply Padding. -->
                        <ScrollViewer x:Name="PART_ContentHost"
                                      VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource TextBoxFocusBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <!-- PART_ContentHost lives INSIDE `border`, so an Opacity here
                                 dimmed the typed text and cost it ClearType. The ComboBox
                                 above gets this right by accident — its Opacity falls on an
                                 empty sibling Border — and this now does it on purpose. -->
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== CheckBox ===================== -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="box"
                                Width="16" Height="16"
                                CornerRadius="3"
                                Background="{DynamicResource BgNeutral}"
                                BorderBrush="{DynamicResource BorderSubtle}"
                                BorderThickness="1"
                                VerticalAlignment="Center">
                            <Path x:Name="check"
                                  Data="M 3.5 8 L 6.5 11 L 12.5 4.5"
                                  Stroke="#1A2028"
                                  StrokeThickness="2"
                                  StrokeEndLineCap="Round"
                                  StrokeStartLineCap="Round"
                                  Visibility="Collapsed"
                                  SnapsToDevicePixels="True"/>
                        </Border>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="box" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== RadioButton ===================== -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Grid Width="16" Height="16" VerticalAlignment="Center">
                            <Ellipse x:Name="ring"
                                     Fill="{DynamicResource BgNeutral}"
                                     Stroke="{DynamicResource BorderSubtle}"
                                     StrokeThickness="1"/>
                            <Ellipse x:Name="dot"
                                     Width="8" Height="8"
                                     Fill="{DynamicResource AccentBrush}"
                                     Visibility="Collapsed"/>
                        </Grid>
                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ring" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- ================= Multiplayer dialog fields =================
         The small multiplayer dialogs (rename a room, enter a room password) used to paint
         their inputs with hardcoded greys and wear the launcher's GOLD buttons, so opening one
         from the navy multiplayer surface jumped to a different application's palette.

         Keyed and applied explicitly, never implicit: the global TextBox style stays the
         launcher's own, and only a control that asks gets the blue treatment. They are here
         rather than copied into each dialog because there are two of them and a third would be
         inevitable. CreateLobbyDialog keeps its own equivalent (MpTitleField) — it is working,
         its field is a different height, and a cosmetic pass is no reason to disturb it.
    ============================================================== -->
    <Style x:Key="MpDialogField" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource MpField}"/>
        <Setter Property="Foreground" Value="{DynamicResource MpTextPrimary}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource MpActionText}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource MpAction}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpRimStrong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="12,0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusRow}">
                        <!-- No Margin="{TemplateBinding Padding}": a TextBox applies its own
                             Padding to the text, so binding it here counts it twice. -->
                        <ScrollViewer x:Name="PART_ContentHost"
                                      VerticalAlignment="Center"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource MpActionRim}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The PasswordBox twin. It needs its own style because WPF ships no implicit one for
         PasswordBox at all, so an unstyled one falls back to the OS light theme. -->
    <Style x:Key="MpDialogPasswordField" TargetType="PasswordBox">
        <Setter Property="Background" Value="{DynamicResource MpField}"/>
        <Setter Property="Foreground" Value="{DynamicResource MpTextPrimary}"/>
        <Setter Property="CaretBrush" Value="{DynamicResource MpActionText}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource MpAction}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource MpRimStrong}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="12,0"/>
        <Setter Property="FontSize" Value="{DynamicResource FontSizeBody}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource RadiusRow}">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      VerticalAlignment="Center"
                                      Focusable="False"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource MpActionRim}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
