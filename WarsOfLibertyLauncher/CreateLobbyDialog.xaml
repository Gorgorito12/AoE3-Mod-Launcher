<Window x:Class="WarsOfLibertyLauncher.CreateLobbyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create a room"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BgBase}"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2a2d34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3a3d44"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#2a2d34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3a3d44"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,12,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="28,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="TitleText"
                   Foreground="{DynamicResource TextPrimary}"
                   FontSize="18"
                   FontWeight="Bold"/>

        <StackPanel Grid.Row="1">
            <TextBlock x:Name="ModLabel" Style="{StaticResource LabelText}"/>
            <!-- Mod selector: lists every installed mod profile so the
                 host can pick which one the room is for. Switching it
                 recomputes HashText below (fingerprint of the selected
                 mod's data files on this PC). -->
            <ComboBox x:Name="ModCombo"
                      Background="#2a2d34"
                      Foreground="White"
                      BorderBrush="#3a3d44"
                      FontSize="13"
                      Padding="8,5"
                      Margin="0,0,0,2"
                      SelectionChanged="ModCombo_SelectionChanged"/>
            <TextBlock x:Name="HashLabel" Style="{StaticResource LabelText}"/>
            <TextBlock x:Name="HashText"
                       Foreground="{DynamicResource TextSecondary}"
                       FontFamily="Consolas, Courier New, monospace"
                       FontSize="11"
                       TextTrimming="CharacterEllipsis"
                       Margin="0,0,0,4"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock x:Name="TitleLabel" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="RoomTitleBox" MaxLength="80"/>

            <Grid Margin="0,4,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="MaxPlayersLabel" Style="{StaticResource LabelText}"/>
                    <ComboBox x:Name="MaxPlayersCombo"
                              Background="#2a2d34"
                              Foreground="White"
                              BorderBrush="#3a3d44"
                              FontSize="12"
                              Padding="8,5">
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                        <ComboBoxItem Content="6"/>
                        <ComboBoxItem Content="7"/>
                        <ComboBoxItem Content="8" IsSelected="True"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock x:Name="PasswordLabel" Style="{StaticResource LabelText}"/>
                    <PasswordBox x:Name="PasswordBox" MaxLength="50"/>
                </StackPanel>
            </Grid>

            <TextBlock x:Name="ErrorText"
                       Foreground="Salmon"
                       FontSize="11"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0"
                       Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button x:Name="CancelButton"
                    Click="CancelButton_Click"
                    Style="{StaticResource SidebarPrimaryButton}"
                    Padding="18,6"
                    MinWidth="110"
                    Margin="0,0,8,0"
                    Background="#3a3d44"/>
            <Button x:Name="CreateButton"
                    Click="CreateButton_Click"
                    Style="{StaticResource SidebarPrimaryButton}"
                    Padding="18,6"
                    MinWidth="120"/>
        </StackPanel>
    </Grid>
</Window>
