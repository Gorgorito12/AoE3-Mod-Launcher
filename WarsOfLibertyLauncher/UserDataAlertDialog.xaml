<Window x:Class="WarsOfLibertyLauncher.UserDataAlertDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Datos de versión anterior detectados"
        Width="600" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BgBase}"
        ShowInTaskbar="False">

    <StackPanel Margin="24">
        <!-- Header -->
        <TextBlock x:Name="HeaderText"
                   FontSize="{StaticResource FontSizeHeading}" FontWeight="SemiBold"
                   Foreground="White"
                   Margin="0,0,0,4"/>
        <TextBlock x:Name="DescriptionText"
                   FontSize="{StaticResource FontSizeBody}"
                   Foreground="#aaa"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <!-- Orange warning panel with the detected path -->
        <Border Background="#3a2a1a"
                BorderBrush="#8c6c3a"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,10"
                Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="WarningTitleText"
                           FontSize="{StaticResource FontSizeCaption}" FontWeight="SemiBold"
                           Foreground="#d4a04a"
                           Margin="0,0,0,4"/>
                <TextBlock x:Name="UserDataPathText"
                           FontSize="{StaticResource FontSizeBody}"
                           Foreground="White"
                           TextWrapping="Wrap"
                           FontFamily="Consolas"/>
                <TextBlock x:Name="SavegameCountText"
                           FontSize="{StaticResource FontSizeCaption}"
                           Foreground="#d4a04a"
                           TextWrapping="Wrap"
                           Margin="0,6,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>
        </Border>

        <!-- Recommendation text -->
        <TextBlock x:Name="RecommendationText"
                   FontSize="{StaticResource FontSizeBody}"
                   Foreground="#ccc"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <!-- Buttons row -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OpenFolderButton"
                    Style="{StaticResource DialogButton}"
                    MinWidth="120"
                    Margin="0,0,8,0"
                    Click="OpenFolderButton_Click"/>
            <Button x:Name="IgnoreButton"
                    Style="{StaticResource DialogButton}"
                    MinWidth="120"
                    Margin="0,0,8,0"
                    Click="IgnoreButton_Click"/>
            <Button x:Name="BackupButton"
                    Style="{StaticResource PrimaryButton}"
                    MinWidth="180"
                    IsDefault="True"
                    Click="BackupButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
